//Global variable to hold all the pages authored.
var pages=[];

$(document).ready(function(){

	$('.add-blog').on('click',function(){
        // XXX write code here to compile handlebars template, generate json data and use it to render the same in HTML 
	});
});